# Analytics

No analytics or personal data collection is implemented.

Candidate events for a consented playtest build: contract accepted, first spindle start, first cut, first overcut, tool break, inspection requested, result grade, retry, exit phase, and voluntary second contract. Event payloads must avoid personal and sensitive data.
