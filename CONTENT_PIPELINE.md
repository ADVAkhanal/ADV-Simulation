# Content pipeline

Contracts will be data-driven records containing customer context, stock, material, target mask, tool constraints, difficulty layers, rewards, failure hooks, and scoring weights. The first contract is authored in code while the schema is validated.

Future content tooling will preview masks, simulate ideal cuts, calculate par scores, and run deterministic regression scenarios before a contract can ship.
